/**
 * ANSI escape code parser
 * interprets terminal color codes (red text)
 * converts codes into colors and styles
 * emits signals for cursor movements, colors, etc
 */

#include <terminal/include/ansiparser.h>