/**
 * app entry point
 * creates the qt app
 * creates the main window
 * loads the MOSS shell process
 * handles closing when shell exits
 */

#include <iostream>

#include "terminal/include/main.h"

int main(){
    return 0;
}